import { makeStyles } from '@material-ui/core/styles';
import { blue } from '@material-ui/core/colors';

export const dialogListStyles = makeStyles({
    dialogTitle: {
        backgroundColor: 'rgba(0,0,0,0.03)',
    },
    avatar: {
        backgroundColor: blue[100],
        color: blue[600],
    },
});