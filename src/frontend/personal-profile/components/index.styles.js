import { makeStyles } from '@material-ui/core/styles';

export const personalProfileStyles = makeStyles(theme => ({
    root: {
        marginTop: theme.spacing(3),
        marginBottom: theme.spacing(3),
        minHeight: 400,
    },
    container: {
        
    },
    paper: {
        padding: theme.spacing(2),
    },
}));