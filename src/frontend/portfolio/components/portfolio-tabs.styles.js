import { makeStyles } from '@material-ui/core/styles';
import { grey } from '@material-ui/core/colors';

export const portfolioTabsStyles = makeStyles({

    root: {
        flexGrow: 1,
        backgroundColor: grey[100],
    },
    iconTab: {
        verticalAlign: 'middle',
    },

});